pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Executing the Build stage'
            }
        }

        stage('Test') {
            steps {
                echo 'Executing the Test stage'
            }
        }

        stage('Deploy') {
            steps {
                echo 'Executing the Deploy stage'
            }
        }
    }
}